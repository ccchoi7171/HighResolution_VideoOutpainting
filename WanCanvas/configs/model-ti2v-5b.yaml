model:
  base_model_id: Wan-AI/Wan2.2-TI2V-5B-Diffusers
  base_pipeline_class: WanImageToVideoPipeline
  strict_dense_mvp: true
  transformer_2: null
  boundary_ratio: null
notes:
  - WanImageToVideoPipeline is the primary runtime surface.
  - FYC tokens enter Wan through encoder_hidden_states_image.
  - If pretrained weights are unavailable locally, WanCanvas falls back to the built-in smoke runtime.
