name: wancanvas
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - git+https://github.com/huggingface/diffusers
      - transformers
      - accelerate
      - safetensors
      - sentencepiece
      - einops
      - peft
      - imageio
      - av
